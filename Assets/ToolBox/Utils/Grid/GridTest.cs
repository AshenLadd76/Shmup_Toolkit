using UnityEngine;

namespace CodeBase.Tools.Grids
{
    public class GridTest : MonoBehaviour
    {
     
        private void Start()
        {
           // var grid = new Grid2D<int>(10,10,1,Vector2.zero,true, (x, y) => x + y);
        }
        
    }
}
